# api/stamp Documentation Rules

Update `README.md` when stamping, authority, key-compatibility, proof, or verification metadata behavior changes.

Update `.env.example` for every new variable.

Update Swagger decorators when endpoints change.

Update `../../AGENTS.md` only when cross-project crypto contracts or service boundaries change.
